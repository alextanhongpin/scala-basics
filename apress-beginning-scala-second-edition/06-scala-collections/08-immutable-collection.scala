// 08-immutable-collection.scala

// A list
val x = List(1,2,3,4)

// Filtering through list
x.filter(a => a % 2 == 0)

// Creating array
val y = Array(1,2,3)

// Creating a map
val z = Map("one" -> 1, "two" -> 2, "three" -> 3)

// Creating a range
0 to 10


(1 to Integer.MAX_VALUE - 1).take(5)