// Array of strings
var books:Array[String] = new Array[String](3)

println(books)

// Simplifying array declarations
var cars = new Array[String](3)

var names = Array("Alpha", "Beta", "Car")
println(names, names(0))